import React from 'react';

export default function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-[#FAF0E6]">
      {/* Logo zone and navbar are rendered separately in index.astro for now */}
      {/* This header component is a wrapper for future enhancements */}
    </header>
  );
}

